[pytest]
testpaths = tests
markers =
    security: Tests for authentication bypass and security vulnerabilities
    lifecycle: Tests for the full video processing lifecycle
    edge_case: Tests for edge cases and error handling
addopts = -v --tb=short
